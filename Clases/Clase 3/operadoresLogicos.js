// && AND => Y
// || OR => O
// ! NOT negación

// not        false
const numero = 0;
const string = "Esto es una cadena de caracteres";
const booleano = true;

if (!(numero && booleano)) {
  console.log("Hola");
}
